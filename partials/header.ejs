<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Fallback favicon -->
    <link rel="icon" href="/icons/icon-192x192.png" type="image/png" />

    <!-- iOS fullscreen behavior -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="MemoryDom">
    <link rel="manifest" href="/manifest.json" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.6.0/css/fontawesome.min.css" integrity="sha384-NvKbDTEnL+A8F/AA5Tc5kmMLSJHUO868P+lDtTpJIeQdGYaUIuLr4lVGOEA1OcMy" crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- ✅ Bootstrap Bundle with Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
   <link rel="stylesheet" href="/css/index.css">
    <link rel="shortcut icon" href="images/social-media.png" type="image/x-icon">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script defer src="/js/client.js"></script>
    <title>capstone project by Angella </title>
</head>

<body>
    
    <nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
        <div class="container">
          <a class="navbar-brand" href="/">MemoryDom</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
              <% if(loggedInUser){ %>

                <li class="nav-item">
                  <a class="nav-link user-profile" href="/userProfile">
                     <img src="<%=loggedInUser.avatar%>" id="avatar" alt="avatar"></a>
                </li>

              <li class="nav-item">
                <div class="dropdown">
                  
                  <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span>Notifications</span>
                    <% if(Notifications.length > 0){ %>
                     <span><%=Notifications.filter((n)=> !n.isRead).length  %></span>
                    <% } %>
                  </button>

                  <div class="dropdown-menu lg" aria-labelledby="dropdownMenuButton">
                    <% Notifications.forEach((n)=>{ %>
                      <% if(n.isRead){ %>
                        No new notifications
                    <% }else{ %>
                     <a class="dropdown-item" href="/notifications/<%=n.notId%>">hey <strong><%= n.loggedInUser %> <%= n.message %>  <p><%=n.timestamp  %></p></strong></a>
                    <% } %>
                    <% }) %>
                  </div>
                </div>
              </li>
        


              <li class="nav-item">
                <a class="nav-link logoutProfile" href="/logout">Log out</a>
              </li>
              
              
              <% }else{ %>
                <li class="nav-item">
                  <a class="nav-link loginBtn active" aria-current="page" href="/login">LOG IN</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link signupBtn" href="/users/signup">Sign up</a>
                </li>
                <% } %>

            </ul>
          </div>
        </div>
      </nav>

      <div class="container newBtnContainer">
        <a id="createNewBtn" href="/new">New 
          <i class="fa-solid fa-plus"></i> 
        </a>
      </div>
      